# ChatSuite Environment Configuration. Choose between dev, qa, and host.
# This sets which environment template to use from config/env/
NX_APP_ENV=dev


# Example overrides for filesystems that don't support Unix ownership changes (SMB/CIFS)
# Copy this file to `.env` and set HOST_UID/HOST_GID to the numeric owner of the mounted data dir
# Example:
# HOST_UID=1026
# HOST_GID=100

